import { StudetDirective } from './studet.directive';

describe('StudetDirective', () => {
  it('should create an instance', () => {
    const directive = new StudetDirective();
    expect(directive).toBeTruthy();
  });
});
